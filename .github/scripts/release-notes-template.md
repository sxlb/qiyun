## 栖云 · Qiyun {{VERSION}} ({{DATE}})

{{CHANGES}}

### 升级提示
- 解压 `qiyun-{{TAG}}.tar.gz` 后执行 `./deploy.sh`（首次自动生成 `.env.deploy` 与密钥）
- 数据库为 `./data/` 下的 SQLite 单文件，**升级前请先备份该目录**；迁移与首次 seed 由容器启动时自动完成

[完整变更对比]({{COMPARE_LINK}})
